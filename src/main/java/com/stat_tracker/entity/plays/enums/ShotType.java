package com.stat_tracker.entity.plays.enums;

public enum ShotType {
    POST_LAYUP, POST_HOOK, POST_FADEAWAY,
    UNDERHAND_LAYUP, OVERHAND_LAYUP, SPIN_LAYUP, EURO_STEP_LAYUP, FLOATER, HOOK,
    JUMP_SHOT, STEP_BACK,
    FREE_THROW,
}
