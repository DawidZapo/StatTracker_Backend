package com.stat_tracker.entity.plays.enums;

public enum ShotType {
    JUMP_SHOT,
    POST_LAYUP, POST_HOOK, POST_FADEAWAY,
    LAYUP_UNDERHAND, LAYUP_OVERHAND, LAYUP_SPIN, LAYUP_EURO_STEP, LAYUP_FLOATER, HOOK,
    STEP_BACK,
    FREE_THROW,
}
